# FableReactionDemo


1. Install NPM-packages:

    via  pnpm:
    ```
    pnpm install
    ```

    or via npm
    ```
    npm install
    ```

2. Start the app:
    ```
    pnpm start
    ```
    or
    ```
    npm start
    ```